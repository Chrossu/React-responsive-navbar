import styled from 'styled-components';

export const StyledBackdrop = styled.div`
  position: fixed;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  position: fixed;
  top: 0;
  left: 0;
  width: 400px;
  z-index: 200;
`;